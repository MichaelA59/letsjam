<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= content_for?(:title) ? yield(:title) : "Untitled" %></title>
    <%= stylesheet_link_tag    "application", media: 'all' %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => false %>
    <%= csrf_meta_tags %>
  </head>
  <body>
  <!-- Nav Bar -->
  <div class="top-bar" id='top-bar'>

    <!-- Left Section of Nav Bar -->
    <div class="top-bar-left">
      <ul class="dropdown menu" data-dropdown-menu>
        <li class='nav-logo'><%= link_to "Let's Jam", root_path %></li>
      </ul>
    </div>

    <!-- Right Section of Nav Bar -->
    <div class="top-bar-right">
      <ul class="menu">
        <!-- Conditional for user signed in or not -->
        <li class="notice"><%= notice %></li>
        <li class="alert"><%= alert %></li>
        <% if user_signed_in? %>
          <li class='right-links'><%= link_to "My Profile", user_path(current_user)%></li>
          <li class='right-links'><%= link_to "Sign Out", destroy_user_session_path, method: :delete%></li>
        <% else %>
          <li class='right-links'><%= link_to 'Log In', new_user_session_path %></li>
          <li class='right-links'><%= link_to 'Sign Up', new_user_registration_path %></li>
        <% end %>
      </ul>
    </div>
  </div>

    <%= yield %>

  </body>
</html>
